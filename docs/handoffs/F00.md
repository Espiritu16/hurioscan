# Handoff — Sprint F00 · Sistema de componentes de interfaz

- Sprint: F00
- Rol: implementation · línea frontend
- Repositorio: hurioscan
- Estado: EN_PROGRESO
- Branch: `sprint/F00`
- base_sha: `bb7ae5b`
- final_sha: pendiente
- worktree_path: `~/hurioscan-F00`
- RFC: `docs/rfcs/F00.md` (aprobado)
- Fuentes: `docs/frontend/experiencia.md`, `docs/frontend/diseno/hurioscan-claude-design.html`, `docs/propuesta/prototipo.html`, RNF-004

## Objetivo

Construir los componentes Blade reutilizables (F00-UT-01 a F00-UT-05): fundación
visual con tokens, componentes de formulario, de datos y de estructura, más la
página de catálogo. Sin pantallas completas ni contratos.

## Decisiones registradas

- **Fuente de tokens de color**: las variables CSS de
  `docs/frontend/diseno/hurioscan-claude-design.html`. Confirmado por el
  Coordinador (2026-08-18) tras verificar que ningún ADR de `docs/decisiones/`
  contiene colores (la referencia del README del diseño está rota; la corrige
  Coordinación en un lote de gobernanza aparte). `prototipo.html` no tiene
  autoridad visual.
- **Solo tema claro**: aprobada por Kevin, 2026-08-18, vía Coordinador. F00
  tokeniza únicamente la paleta clara del diseño; el tema oscuro queda fuera
  del horizonte y sería trabajo planificado aparte.
- **Tailwind CSS v4**: los tokens se declaran con `@theme` en
  `resources/css/app.css`; no existe `tailwind.config.js` en v4. La
  "configuración de Tailwind" de F00-UT-01 se materializa ahí (interpretación
  trivial documentada, validada por el Coordinador).
- **Tipografía del diseño**: `Archivo` (sans) e `IBM Plex Mono` (mono),
  extraídas del design HTML. Se cargan con el plugin de fuentes de
  `laravel-vite-plugin` (bunny) ya presente en `vite.config.js`, reemplazando
  la `Instrument Sans` del scaffold.
- **Menú lateral y rutas**: los enlaces consumen rutas **por nombre** y se
  protegen con `Route::has()` — un nombre aún no declarado se muestra
  deshabilitado en lugar de romper. La línea frontend no edita
  `routes/web.php`.
- **Nombres de ruta canónicos**: fijados por Arquitectura en
  `docs/frontend/integracion.md`. Los del menú coinciden con esa lista.
  `pacientes.detalle` (para `/pacientes/{id}`, que consume F06) fue
  **ratificado por Arquitectura** el 2026-08-19 siguiendo el patrón de
  `sesiones.detalle` y `documentos.detalle`; Coordinación lo agrega a la lista
  en su próximo lote de gobernanza.

## Restricciones del despacho

- No editar `routes/web.php`; la ruta `/componentes` (F00-UT-05) se solicita
  al Coordinador.
- Push solo con confirmación explícita de Kevin.
- Terminar en EN_VALIDACION; el cierre lo decide el Coordinador tras QA.

## Unidades de trabajo

| ID | Estado | Evidencia |
|---|---|---|
| F00-UT-01 | HECHA | tokens en `resources/css/app.css` (`@theme`), fuentes Archivo/IBM Plex Mono en `vite.config.js`; build ok — commit `bf1f00a` |
| F00-UT-02 | HECHA | `<x-campo>`, `<x-boton>` (3 variantes), `<x-buscador>`; error accesible (aria-invalid/aria-describedby/role=alert); tests en `ComponentesFormularioTest` — commit `75d3ca4` |
| F00-UT-03 | HECHA | `<x-tabla>` (+`<x-tabla.encabezado>` con aria-sort), `<x-etiqueta-estado>` (4 colores semánticos, sin acento), `<x-tarjeta-indicador>`, `<x-estado-vacio>`; tests en `ComponentesDatosTest` — commit `bca26a7` |
| F00-UT-04 | HECHA | `<x-layout>`, `<x-menu-lateral>` (visibilidad por rol; rutas por nombre con `Route::has`), `<x-barra-paciente>` (sticky), `<x-indicador-paso>` (miga + «Paso n de m» móvil); tests en `ComponentesEstructuraTest` — commit `b8f7f23` |
| F00-UT-05 | BLOQUEADA (solo ruta) | vista `componentes/catalogo.blade.php` completa con todas las variantes + test de render — commit `8c0244a`; falta montar la ruta `/componentes` (solo local) en `routes/web.php` — ver bloqueante abajo |

## Corrección posterior en `<x-tabla>` (detectada en F02)

El componente desbordaba la página en 360 px al reutilizarse dentro de un
contenedor flex constreñido. Dos causas sumadas:

1. el div con `overflow-x-auto` heredaba `min-width: auto` como ítem flex y
   crecía hasta el ancho de la tabla en vez de desplazarse por dentro;
2. el `sr-only` de un encabezado, posicionado en absoluto y sin un contexto de
   posicionamiento cercano, tomaba como referencia un ancestro de más arriba y
   escapaba al recorte del contenedor.

Corregido con `w-full min-w-0 relative` en el propio componente, así que
alcanza a todas las vistas que lo usan.

**Por qué el catálogo no lo detectó:** aquí la tabla no está dentro de un
contenedor flex constreñido, así que pasaba la verificación en 360 px. El
defecto solo aparecía al usar el componente en una vista real — buena razón
para que QA valide sobre vistas, no sobre componentes en aislamiento.

## Bloqueante abierto — montaje de la ruta `/componentes`

El waiver de rutas registrado en el estado global (delegación VIGENTE del
2026-08-19) autoriza a esta línea a **agregar** rutas nombradas. Al intentar
aplicarlo, la escritura de `routes/web.php` fue **rechazada por el control de
permisos de la sesión**, y la instrucción directa del usuario a esta sesión es
literal: no editar ese archivo bajo ninguna circunstancia.

No se buscó ningún rodeo. El cambio pendiente es exactamente:

```php
if (app()->environment('local')) {
    Route::view('/componentes', 'componentes.catalogo')->name('componentes');
}
```

Lo resuelve o bien el usuario levantando la restricción en la sesión, o bien
quien tenga autoridad sobre `routes/web.php`. Escalado a Coordinación el
2026-08-19.

## Verificación responsive (RNF-004)

Catálogo renderizado a HTML estático y verificado en navegador (DOM real):

- 360 px: sin desborde horizontal (`scrollWidth == innerWidth`), la tabla se
  desplaza dentro de su contenedor, menú en modo hamburguesa con
  `aria-expanded` correcto al alternar, indicador «Paso 2 de 4» visible.
- 768 px y 1024 px: sin desborde horizontal; en 1024 el menú lateral fijo.
- 1440 px: sin desborde; barra de paciente sticky verificada en `top: 0` con
  la página desplazada.

## Checkpoints

### 2026-08-19 — UT-01 a UT-04 hechas; UT-05 a falta de la ruta
- SHA actual: `8c0244a`
- Completado: tokens y fuentes (UT-01); once componentes Blade con sus tests
  (UT-02/03/04); página de catálogo con test de render (UT-05, vista lista).
  Verificación responsive de RNF-004 en los cuatro anchos (ver sección).
- Pendiente: ruta `/componentes` (la declara Coordinación); con ella, UT-05
  queda verificable en navegador por QA.
- Bloqueantes: solo la ruta anterior.
- Último resultado de pruebas: pint `passed`; `php artisan test` 19 tests,
  19 passed (64 aserciones); `pnpm build` ok (2026-08-19).

### 2026-08-18 — apertura
- SHA actual: `bb7ae5b` (= base_sha, working tree limpio)
- Completado: reconstrucción de contexto; dudas de paleta y tema resueltas por
  el Coordinador/Kevin; handoff creado.
- Pendiente: F00-UT-01 a 05.
- Bloqueantes: ninguno (la ruta de UT-05 se pedirá al llegar).
- Último resultado de pruebas: scaffold en verde según `AGENTS.md`
  (lint `passed`, 2 tests `passed`, build ok — 2026-08-18).

# Handoff — Sprint F05 · Revisión del OCR y cierre

- Sprint: F05
- Rol: implementation · línea frontend
- Repositorio: hurioscan
- Estado: **EN_VALIDACION con un pendiente** — cerrado salvo el montaje de rutas
- Branch: `sprint/F00` (modalidad de cadena)
- worktree_path: `~/hurioscan-F00`
- RFC: `docs/rfcs/F05.md` (aprobado por Kevin, 2026-08-19)
- Fuentes: `docs/frontend/experiencia.md`, `docs/contratos/documentos.md`,
  `docs/contratos/digitalizacion.md`, RNF-004, RNF-012

## Unidades de trabajo

| ID | Estado | Evidencia |
|---|---|---|
| F05-UT-01 | HECHA | `ServicioDocumentosDoble` con los cinco estados de revisión y el conflicto de versión; incluye un documento cuyo texto extraído lleva un payload XSS a propósito. |
| F05-UT-02 | HECHA a falta de montaje | `RevisionOcr`: imagen y texto en paralelo desde 1024 px y apilados en 360/768 (verificado midiendo la posición real de ambos paneles); `PENDIENTE_OCR` explica que el OCR sigue corriendo; el conflicto muestra el texto vigente y ofrece las dos salidas sin reenviar en silencio. |
| F05-UT-03 | HECHA a falta de montaje | marcar avanza a la hoja siguiente; el contador refleja el progreso real (3/5 al inicio, 4/5 tras marcar); una transición no permitida muestra su motivo. |
| F05-UT-04 | HECHA a falta de montaje | `CierreSesion` muestra hojas por estado y por tipo antes de confirmar, y una hoja ilegible no bloquea el cierre; `HojasIlegibles` saca la hoja de la cola tras reabrirla. |

## RNF-012 — el texto extraído nunca se ejecuta

El doble guarda el payload tal cual (escapar al guardar corrompería el
documento; el contrato lo exige así). La vista no usa `{!! !!}` en ningún
punto, de modo que Blade escapa por defecto. Hay un test que verifica sobre el
HTML renderizado que `<script>alert("xss")</script>` **no** aparece como
etiqueta y que sí aparece como `&lt;script&gt;`.

## Pendiente de montaje

| Nombre canónico | Componente al que apuntará |
|---|---|
| `sesiones.revision` (`/sesiones/{id}/revision`) | `App\Dominios\Documentos\Componentes\RevisionOcr` |
| `sesiones.cierre` (`/sesiones/{id}/cierre`) | `App\Dominios\Digitalizacion\Componentes\CierreSesion` |
| `ilegibles` | `App\Dominios\Documentos\Componentes\HojasIlegibles` |

## Decisiones e interpretaciones

- Resolver un conflicto de versión son dos acciones explícitas —"usar el texto
  vigente" o "conservar el mío"—, nunca una resolución automática. Tomar el
  vigente lo carga en el editor y avisa que hay que revisarlo antes de guardar,
  en vez de guardarlo por su cuenta.
- El contador de progreso cuenta hojas en estado terminal (`CORRECTA`,
  `CORREGIDA`, `ILEGIBLE`), que es el progreso que la persona percibe.

## Checkpoints

### 2026-08-19 — F05 construido y verificado
- Completado: las cuatro unidades en lo que no depende de rutas.
- Pendiente: el montaje de las tres rutas de la tabla de arriba.
- Último resultado de pruebas: pint `passed`; `php artisan test` 62 tests, 62
  passed; `pnpm build` ok. Responsive verificado en 12 combinaciones.

## Validación QA — 2026-08-19

- Objeto: `hurioscan@9576f686bf241ebfa9a7d6ca30d953d7f7db7720`
- Entorno: worktree propio de QA en HEAD detached, dobles activados, servidor real y navegador en 360/768/1024/1440. QA no escribió ni commiteó nada en el árbol objetivo.
- Reproducido de forma independiente: `pint` passed y 125/125 tests (494 aserciones); las dieciséis rutas sirven.
- Seguridad de aplicación (`seguridad-validacion`, modo dirigido): **SIN_HALLAZGOS_BLOQUEANTES** para la cadena. Se revalidó el 404 de las rutas parametrizadas incluso con `APP_DEBUG=true` y con entradas manipuladas, sin exponer traza.
- **Alcance:** el veredicto vale solo para ese SHA y es sobre interfaz contra dobles. Ningún sprint pasa a `COMPLETADO` hasta su punto de integración con el sprint `B` correspondiente, como declara el roadmap.

### Veredicto: APROBADO

Texto extraído escapado sin inyección, viviendo como valor del textarea. `PENDIENTE_OCR` informa que el proceso sigue en curso en vez de mostrar un panel vacío. En 360 px los dos paneles se apilan a ancho completo. El conflicto de versión ofrece sus dos salidas, probadas una por una, y nunca reenvía en silencio. Marcar una hoja avanza a la siguiente y actualiza el contador. Una transición no permitida muestra su motivo en una región con `role="status"`. Una hoja ilegible no bloquea el cierre.

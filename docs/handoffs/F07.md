# Handoff — Sprint F07 · Panel de avance y administración

- Sprint: F07
- Rol: implementation · línea frontend
- Repositorio: hurioscan
- Estado: **EN_VALIDACION con un pendiente** — cerrado salvo el montaje de rutas
- Branch: `sprint/F00` (modalidad de cadena)
- worktree_path: `~/hurioscan-F00`
- RFC: `docs/rfcs/F07.md` (aprobado por Kevin, 2026-08-19)
- Fuentes: `docs/frontend/experiencia.md`, `docs/contratos/digitalizacion.md`,
  `docs/contratos/usuarios.md`

## Unidades de trabajo

| ID | Estado | Evidencia |
|---|---|---|
| F07-UT-01 | HECHA | los dobles de Digitalización y Usuarios cubren agregados, usuarios y filas de auditoría; el caso de `totalFoldersAcervo` sin configurar está en el doble de forma explícita (`null`). |
| F07-UT-02 | HECHA a falta de montaje | `PanelAvance`: sin total configurado muestra el avance absoluto y **ninguna barra ni porcentaje**; hay test que verifica que no aparece ningún «% de la meta». |
| F07-UT-03 | HECHA a falta de montaje | `AdministracionUsuarios`: quitarse el propio rol o desactivarse muestra por qué se rechaza; test sobre el HTML de que no aparece ningún hash de contraseña. |
| F07-UT-04 | HECHA a falta de montaje | `ConsultaAuditoria`: los filtros viajan en la URL; test de que no se filtra texto de documento ni contraseña; una acción del sistema se muestra sin usuario. |

## Verificación responsive (RNF-004)

12 combinaciones vista×ancho sin desborde horizontal.

## Pendiente de montaje

| Nombre canónico | Componente al que apuntará |
|---|---|
| `avance` | `App\Dominios\Digitalizacion\Componentes\PanelAvance` |
| `usuarios` | `App\Dominios\Usuarios\Componentes\AdministracionUsuarios` |
| `auditoria` | `App\Dominios\Usuarios\Componentes\ConsultaAuditoria` |

El alcance por rol (que `consulta` no vea el panel de avance, y que solo
`administrador` llegue a usuarios y auditoría) lo aplica el middleware de la
ruta al montarla, y el menú lateral ya oculta esas opciones. Ocultar es ayuda
de interfaz; el backend revalida siempre (RNF-013).

## Decisiones e interpretaciones

- La regla de "no quitarse el propio rol" se prueba contra el administrador de
  ejemplo del doble (id 3). En producción el actor lo resuelve la sesión, no la
  vista.
- El panel dibuja la barra de progreso solo cuando hay porcentaje real; con el
  total sin configurar se dice explícitamente en vez de insinuar un 0 %.

## Checkpoints

### 2026-08-19 — F07 construido y verificado
- Completado: las cuatro unidades en lo que no depende de rutas.
- Pendiente: el montaje de las tres rutas de la tabla de arriba.
- Último resultado de pruebas: pint `passed`; `php artisan test` 84 tests, 84
  passed; `pnpm build` ok.

---
id: F07
name: Panel de avance y administración
role: implementation
line: frontend
repository: hurioscan
status: EN_VALIDACION
branch: sprint/F00
base_sha: bb7ae5ba014ed4e108797ef9e6b798541f0f21c9
final_sha: 9576f686bf241ebfa9a7d6ca30d953d7f7db7720
outcome: terminado
veredicto_qa: APROBADO
verificacion: pint passed; 125/125 tests (494 aserciones); las dieciseis rutas sirven — QA, 2026-08-19
worktree_path: retirado el 2026-08-20 — el contenido vive en la rama `sprint/F00`
rfc: docs/rfcs/F07.md
updated_at: 2026-08-21
---
# Handoff — Sprint F07 · Panel de avance y administración

> **Metadatos añadidos el 2026-08-21 por Coordinación.** El frontmatter de arriba
> no lo escribió la sesión que ejecutó este sprint: se añadió al retomar el
> proyecto, para que el estado pueda leerse sin interpretar prosa y para que
> `verify_project.py` pueda validarlo. **Cada campo se derivó de lo que este
> documento y Git ya demuestran**, y ninguno se inventó: `final_sha` es el objeto
> exacto sobre el que QA emitió su veredicto, citado más abajo en este mismo
> archivo y verificado como commit existente y contenido en `develop`;
> `base_sha` es el punto del que se creó el worktree de la cadena; `status`
> coincide con `docs/roadmap.md`. El campo `outcome` **es el único derivado y no
> literal**: ninguna sesión lo declaró, porque estos sprints se ejecutaron bajo el
> modelo de chats de rol anterior al despacho por subagentes, que no lo exigía. Se
> deriva de que todas las unidades de trabajo figuran HECHAS y de que el veredicto
> QA registrado abajo es favorable. Si esa derivación se considera indebida, el
> valor correcto es `null`, no otro outcome.

- Sprint: F07
- Rol: implementation · línea frontend
- Repositorio: hurioscan
- Estado: **EN_VALIDACION con un pendiente** — cerrado salvo el montaje de rutas
- Branch: `sprint/F00` (modalidad de cadena)
- worktree_path: `**retirado el 2026-08-20** — el contenido vive en la rama `sprint/F00` (`git show sprint/F00:<ruta>`)
- RFC: `docs/rfcs/F07.md` (aprobado por Kevin, 2026-08-19)
- Fuentes: `docs/frontend/experiencia.md`, `docs/contratos/digitalizacion.md`,
  `docs/contratos/usuarios.md`

## Unidades de trabajo

| ID | Estado | Evidencia |
|---|---|---|
| F07-UT-01 | HECHA | los dobles de Digitalización y Usuarios cubren agregados, usuarios y filas de auditoría; el caso de `totalFoldersAcervo` sin configurar está en el doble de forma explícita (`null`). |
| F07-UT-02 | HECHA a falta de montaje | `PanelAvance`: sin total configurado muestra el avance absoluto y **ninguna barra ni porcentaje**; hay test que verifica que no aparece ningún «% de la meta». |
| F07-UT-03 | HECHA a falta de montaje | `AdministracionUsuarios`: quitarse el propio rol o desactivarse muestra por qué se rechaza; test sobre el HTML de que no aparece ningún hash de contraseña. |
| F07-UT-04 | HECHA a falta de montaje | `ConsultaAuditoria`: los filtros viajan en la URL; test de que no se filtra texto de documento ni contraseña; una acción del sistema se muestra sin usuario. |

## Verificación responsive (RNF-004)

12 combinaciones vista×ancho sin desborde horizontal.

## Pendiente de montaje

| Nombre canónico | Componente al que apuntará |
|---|---|
| `avance` | `App\Dominios\Digitalizacion\Componentes\PanelAvance` |
| `usuarios` | `App\Dominios\Usuarios\Componentes\AdministracionUsuarios` |
| `auditoria` | `App\Dominios\Usuarios\Componentes\ConsultaAuditoria` |

El alcance por rol (que `consulta` no vea el panel de avance, y que solo
`administrador` llegue a usuarios y auditoría) lo aplica el middleware de la
ruta al montarla, y el menú lateral ya oculta esas opciones. Ocultar es ayuda
de interfaz; el backend revalida siempre (RNF-013).

## Decisiones e interpretaciones

- La regla de "no quitarse el propio rol" se prueba contra el administrador de
  ejemplo del doble (id 3). En producción el actor lo resuelve la sesión, no la
  vista.
- El panel dibuja la barra de progreso solo cuando hay porcentaje real; con el
  total sin configurar se dice explícitamente en vez de insinuar un 0 %.

## Checkpoints

### 2026-08-19 — F07 construido y verificado
- Completado: las cuatro unidades en lo que no depende de rutas.
- Pendiente: el montaje de las tres rutas de la tabla de arriba.
- Último resultado de pruebas: pint `passed`; `php artisan test` 84 tests, 84
  passed; `pnpm build` ok.

## Validación QA — 2026-08-19

- Objeto: `hurioscan@9576f686bf241ebfa9a7d6ca30d953d7f7db7720`
- Entorno: worktree propio de QA en HEAD detached, dobles activados, servidor real y navegador en 360/768/1024/1440. QA no escribió ni commiteó nada en el árbol objetivo.
- Reproducido de forma independiente: `pint` passed y 125/125 tests (494 aserciones); las dieciséis rutas sirven.
- Seguridad de aplicación (`seguridad-validacion`, modo dirigido): **SIN_HALLAZGOS_BLOQUEANTES** para la cadena. Se revalidó el 404 de las rutas parametrizadas incluso con `APP_DEBUG=true` y con entradas manipuladas, sin exponer traza.
- **Alcance:** el veredicto vale solo para ese SHA y es sobre interfaz contra dobles. Ningún sprint pasa a `COMPLETADO` hasta su punto de integración con el sprint `B` correspondiente, como declara el roadmap.

### Veredicto: APROBADO

Sin total configurado no aparece ninguna barra ni un porcentaje engañoso. El hash de contraseña no aparece en ninguna vista, con la página listando usuarios reales. Quitarse el propio rol de administrador se rechaza explicando por qué. La consulta de auditoría no filtra texto de documento ni número de documento de identidad.

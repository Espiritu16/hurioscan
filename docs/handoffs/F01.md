# Handoff — Sprint F01 · Acceso y navegación

- Sprint: F01
- Rol: implementation · línea frontend
- Repositorio: hurioscan
- Estado: **BLOQUEADO**
- Branch: `sprint/F01` — se ejecuta en la rama `sprint/F00` por la modalidad de
  cadena ordenada en la delegación (un solo worktree, un commit por sprint)
- base_sha: `bb7ae5b` (worktree creado ahí; la cadena parte de `be0d273`, el
  merge de `origin/develop` que trajo la línea base aprobada)
- final_sha: pendiente
- worktree_path: `~/hurioscan-F00`
- RFC: `docs/rfcs/F01.md` (aprobado por Kevin, 2026-08-19)
- Fuentes: `docs/frontend/experiencia.md` (flujo "Acceder al sistema"),
  `docs/frontend/integracion.md`, `docs/contratos/usuarios.md`,
  `docs/requisitos/actores-permisos.md`

## Objetivo

Pantalla de acceso, layout aplicado y navegación con visibilidad por rol. No
implementa autenticación ni autorización reales: eso es B01.

## Unidades de trabajo

| ID | Estado | Evidencia |
|---|---|---|
| F01-UT-01 | **BLOQUEADA** | doble implementado en `app/Compartido/Dobles/SesionSimulada.php` con los tres roles y los tres modos de fallo, cubierto por tests (commit `afe7fe6`). **No cierra su criterio**: "se activa solo por configuración" exige una interfaz fuera de `Dobles/` y un binding por entorno, ambos en rutas que esta línea no puede escribir. Ver bloqueante abajo. |
| F01-UT-02 | HECHA a falta de montaje | vista `dominios/usuarios/acceder.blade.php` con idle, enviando y error; mensaje único que no revela si el correo existe; foco devuelto al primer campo marcado (verificado en navegador: `document.activeElement` es `email`); responsive 360/768/1024/1440 sin desborde. Tests en `PantallaAccesoTest`. Commit `afe7fe6`. Falta montar la ruta `acceder`. |
| F01-UT-03 | PENDIENTE | el layout y el menú por rol ya existen desde F00 (`<x-layout>`, `<x-menu-lateral>`), pero "aplicado" exige montarlo en rutas reales, que es el mismo bloqueante. |

## Bloqueantes

### 1. Interfaz y activación por configuración de los dobles (estructural)

Afecta al UT-01 de **todos** los sprints de la cadena, no solo a F01.

El criterio pide que el doble se active solo por configuración, y `AGENTS.md`
añade que ningún doble se referencie desde código de producción. Cumplir ambas
exige que el código de producción dependa de una **interfaz** y que un binding
elija implementación según el entorno. El precedente canónico es ADR-0002:
`App\Compartido\Ocr\MotorOcr`, es decir la interfaz en `app/Compartido/<área>/`.

`AGENTS.md` prohíbe a esta línea escribir `app/Compartido/` fuera de `Dobles/`,
y también `config/` y `app/Providers/`. No se puede crear ni la interfaz ni el
binding. Poner la interfaz dentro de `Dobles/` haría que el código de
producción importara de ese namespace, que es justo lo prohibido.

No se resolvió por criterio propio porque fija la firma que B01–B07
implementarán: es autoría de Arquitectura. Escalado a Coordinación el
2026-08-19 con tres opciones (interfaces creadas por backend/Arquitectura;
waiver de ruta para esta línea; o aceptar `Dobles/Contratos/` como deuda
declarada). Sin decisión, la cadena se detiene aquí por la condición del
propio despacho.

### 2. Montaje de rutas

Mismo bloqueante que cierra F00-UT-05, descrito en `docs/handoffs/F00.md`. La
cadena completa necesitaría los nombres canónicos `acceder`, `salir`, `avance`,
`pacientes`, `pacientes.detalle`, `sesiones.pendientes`, `sesiones.detalle`,
`sesiones.revision`, `sesiones.cierre`, `buscar`, `documentos.detalle`,
`ilegibles`, `usuarios`, `auditoria`, más `componentes` (solo local).

## Decisiones e interpretaciones

- El doble de sesión devuelve el **mismo resultado** para credencial inválida,
  usuario inexistente y usuario inactivo, porque `docs/contratos/usuarios.md`
  exige un único `NO_AUTENTICADO` que no revele qué correos están registrados.
- El destino tras acceder depende del rol: `consulta` no tiene acceso al panel
  de avance (contrato de `GET /avance`), así que entra por `pacientes`.
  Interpretación trivial derivada de la matriz de permisos, documentada aquí.
- El componente `<x-campo>` de F00 se extendió con un estado `invalido` sin
  mensaje propio, para el formulario de acceso, donde el motivo se anuncia una
  sola vez. Es reutilización, no duplicación.

## Checkpoints

### 2026-08-19 — F01 detenido en su primer bloqueante
- SHA actual: `afe7fe6`
- Completado: doble de sesión con sus tests; pantalla de acceso con sus tres
  estados, verificada en los cuatro anchos y con el foco comprobado en
  navegador real.
- Pendiente: activación del doble por configuración (UT-01), montaje de rutas
  (UT-02/UT-03).
- Bloqueantes: los dos descritos arriba, ambos escalados a Coordinación.
- Último resultado de pruebas: pint `passed`; `php artisan test` 25 tests, 25
  passed (85 aserciones).
- **La cadena no continúa a F02** mientras F01 no cierre sus criterios, según
  la condición del despacho.

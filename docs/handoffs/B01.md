# Handoff — Sprint B01 · Fundación, acceso y roles

- Sprint: B01
- Rol: implementation · línea backend
- Repositorio: hurioscan
- Estado: EN CURSO
- Branch: `sprint/B01`
- base_sha: `f8d2c94e326a328d8bac441f48fa8b092ee13417`
- final_sha: pendiente
- worktree_path: `~/hurioscan-B01`
- RFC: `docs/rfcs/B01.md` (aprobado por Kevin, 2026-08-20)
- Fuentes: `docs/requisitos/rf.md` (RF-011), `docs/requisitos/rnf.md` (RNF-005,
  RNF-013), `docs/requisitos/actores-permisos.md`, `docs/persistencia/modelo.md`
  (Usuario, MIG-001), `docs/errores/manejo-errores.md`,
  `docs/contratos/usuarios.md`, `docs/contratos/servicios-aplicacion.md`

## Objetivo

Dejar el proyecto ejecutable con la estructura por dominios, la tabla base,
acceso con usuario y contraseña, los tres roles y el mecanismo de autorización
que rechaza por defecto. No entrega pantallas: las construyó F01.

## Orden de ejecución

UT-01 → UT-02 → UT-03 → UT-04, en secuencia. La tabla del RFC declara UT-02 y
UT-03 paralelizables entre sí, pero dos de las tres pruebas mínimas de RNF-013
que UT-03 debe pasar exigen autenticarse, y la autenticación la construye
UT-02; la propia nota del RFC lo deja anotado.

## Unidades de trabajo

| ID | Estado | Evidencia |
|---|---|---|
| UT-01 | **HECHA** | ver abajo |
| UT-02 | en curso | — |
| UT-03 | pendiente | — |
| UT-04 | pendiente | — |

## Checkpoints

### UT-01 — Fundación compartida

**Entregado**

- `database/migrations/0001_01_01_000000_create_usuarios_table.php` (MIG-001):
  tabla `usuarios` con `nombre`, `email` único, `password`, `rol`, `activo`,
  `remember_token`, `created_at`/`updated_at` y `deleted_at` en `timestamptz`,
  índice sobre `rol` y restricción `CHECK` del conjunto cerrado de roles.
- `app/Dominios/Usuarios/Usuario.php` — entidad Usuario dentro de su dominio,
  con soft-delete y `password` hasheado.
- `database/factories/UsuarioFactory.php`, con estados `conRol()` e
  `inactivo()`.
- `app/Compartido/Persistencia/InstantesEnUtc.php` — instantes con
  desplazamiento explícito (ver «Hallazgo» abajo).
- `config/app.php`: `timezone` fija en UTC **sin variable de entorno**, y
  `zona_visualizacion` (`America/Lima`) como decisión de presentación aparte.
- `config/database.php`: la conexión PostgreSQL abre su sesión en UTC.
- `config/auth.php`: el proveedor apunta a `App\Dominios\Usuarios\Usuario`.
- `.env.example`: `APP_TIMEZONE` (inerte, `config/app.php` no lo leía) sustituido
  por `APP_ZONA_VISUALIZACION`, con la explicación de por qué la zona interna no
  es configurable.

**Evidencia**

`php artisan migrate` desde base PostgreSQL 18.3 limpia:

```
INFO  Preparing database.
Creating migration table .. 23.54ms DONE
INFO  Running migrations.
0001_01_01_000000_create_usuarios_table .. 36.36ms DONE
0001_01_01_000001_create_cache_table .. 5.36ms DONE
0001_01_01_000002_create_jobs_table .. 10.84ms DONE
```

`\d usuarios` sobre la base resultante devuelve exactamente el schema aprobado
en `docs/persistencia/modelo.md`: los diez campos con sus tipos, el único de
`email`, el índice de `rol` y
`CHECK (rol IN ('operador','consulta','administrador'))`.

`tests/Feature/Backend/FundacionUtcTest.php` — 2 pruebas, verde en los dos
motores (sqlite en la suite rápida, PostgreSQL 18.3 en la corrida de
integración).

**Hallazgo: el instante se desplazaba al escribirlo, y nada fallaba**

La prueba de RNF-005 falló en PostgreSQL antes de existir la corrección, y por
un motivo que no se ve leyendo el código: el formato de fecha por defecto de
Eloquent (`Y-m-d H:i:s`) no lleva desplazamiento, así que PostgreSQL interpreta
lo que recibe **en la zona de la sesión**. Con la sesión en `America/Lima`, un
instante de `2026-08-20 03:30:00 UTC` quedaba guardado como `08:30 UTC`. La
lectura deshacía el mismo desplazamiento, de modo que la aplicación mostraba la
hora correcta y la fila estaba mal. El mismo hueco existe al leer: sin
desplazamiento, Carbon interpreta la cadena en la zona por defecto de PHP.

`InstantesEnUtc` cierra los dos huecos con una sola línea: el instante viaja con
su desplazamiento (`Y-m-d H:i:sP`) y deja de depender de ninguna zona de sesión.

**Decisiones de interpretación**

1. **MIG-001 se materializó reescribiendo la migración del scaffold**, renombrada
   a `create_usuarios_table`, en vez de encadenarle un `ALTER` posterior. El plan
   describe el cambio como «agregar … a la tabla `users` del scaffold, renombrada
   a `usuarios`» y declara que no hay datos; el estado final del schema es el
   aprobado, que es lo que MIG-001 fija, y una tabla creada ya con su forma final
   se lee y se revierte mejor que una creada mal y corregida a continuación.
2. **Se eliminaron `app/Models/User.php` y `database/factories/UserFactory.php`.**
   `app/Models/` no figura en las rutas escribibles de la línea backend en
   `AGENTS.md`, pero tampoco en las prohibidas, y domain-first no admite una
   carpeta técnica por capa. Dejar ese modelo habría sido dejar una clase
   apuntando a una tabla que ya no existe. Queda declarado para que Coordinación
   lo vea al revisar el PR.
3. **`sessions.user_id` conserva su nombre en inglés**: lo escribe el manejador
   de sesiones de Laravel con ese nombre exacto. Es infraestructura del
   framework, no un campo del dominio.
4. **La restricción `CHECK` de `rol` solo se crea en PostgreSQL.** SQLite no
   admite agregar restricciones con `ALTER TABLE`. Está declarado en la propia
   migración: SQLite es la base de retroalimentación rápida de la suite, el motor
   del proyecto es PostgreSQL y sobre él corre la suite Feature en CI.

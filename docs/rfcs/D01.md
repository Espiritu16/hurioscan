## RFC — D01 · Pipeline de integración continua

- RF que implementa: ninguno directamente — materializa las obligaciones de verificación que `AGENTS.md` declara en «CI por rama» y «Comandos de verificación»
- Alcance de este sprint: crear los flujos de trabajo de GitHub Actions que ejecutan las verificaciones ya declaradas, en cada pull request hacia `develop` y hacia `main`. **No** incluye despliegue, artefactos ni ambientes: el proyecto no tiene producción comprometida y `AGENTS.md` declara el proveedor como no decidido.
- Documentos relacionados: `AGENTS.md` — secciones «CI por rama», «Comandos de verificación» y «Operación DevOps/Release»
- Contratos que toca: ninguno
- **Depende de: nada.** Los comandos ya están declarados y verificados localmente en verde el 2026-08-18. Puede ejecutarse de inmediato, en paralelo con F00 y B01.
- Rol: `devops`
- Estado: aprobado
- Aprobado por: Kevin — fecha: 2026-08-18

## Unidades de trabajo — DevOps

| ID | Resultado observable | Deriva de | Depende de | Paralelizable con | Interfaz fijada | Criterio de cierre |
|---|---|---|---|---|---|---|
| D01-UT-01 | Flujo de verificación para pull requests hacia `develop`: lint, tests y build | `AGENTS.md` § CI por rama | ninguna | D01-UT-02 | `.github/workflows/` | un PR hacia `develop` ejecuta `./vendor/bin/pint --test`, `php artisan test` y `pnpm build`; un fallo real de cualquiera de los tres marca el check en rojo, verificado provocando el fallo a propósito |
| D01-UT-02 | Flujo de verificación para pull requests hacia `main`: lo anterior más la suite de integración sobre PostgreSQL | `AGENTS.md` § CI por rama | ninguna | D01-UT-01 | servicio de PostgreSQL en el flujo | el servicio levanta, las migraciones aplican desde base limpia y `php artisan test --testsuite=Feature` corre contra él; sin migraciones que aplicar todavía, el flujo debe pasar igualmente en verde |
| D01-UT-03 | Caché de dependencias de Composer y pnpm | ninguna | D01-UT-01 | ninguna | claves de caché del flujo | una segunda ejecución sin cambios en los lockfiles reutiliza la caché, verificado comparando duraciones |

**Sobre `AGENTS.md`:** este sprint **no** lo edita — el rol `devops` no tiene autoridad sobre la gobernanza. Al cerrar D01, el Coordinador reemplaza los `previsto en D01` de «CI por rama» y «Comandos de verificación» por la referencia al flujo real, como acción de gobernanza aparte.

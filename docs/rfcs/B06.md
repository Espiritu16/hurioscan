## RFC — B06 · Consulta del archivo

- RF que implementa: RF-007, RF-008, RF-009
- Alcance de este sprint: la búsqueda por contenido con su índice de texto completo, la línea de tiempo de documentos de un paciente con sus filtros, y el visor de documento con la entrega controlada de la imagen original. Es el sprint que hace visible el valor del producto.
- Documentos relacionados: `docs/requisitos/rf.md` (RF-007, RF-008, RF-009), `docs/requisitos/rnf.md` (RNF-001, RNF-004, RNF-011, RNF-012), `docs/persistencia/modelo.md` (columna `busqueda`, MIG-005), `docs/decisiones/0004-busqueda-full-text-en-postgresql.md`, `docs/frontend/experiencia.md`
- Contratos que toca: `docs/contratos/documentos.md` — `GET /buscar`, `GET /documentos/{id}`, `GET /documentos/{id}/imagen`; `docs/contratos/pacientes.md` — `GET /pacientes/{id}`
- Estado: propuesto
- Aprobado por: pendiente — fecha: pendiente
- Paralelizable con: B07 (declarado en `docs/roadmap.md`) — no comparten archivos: este sprint trabaja el dominio `Documentos` y B07 los agregados de `Digitalizacion` y el dominio `Usuarios`

> **Este sprint no entrega pantallas.** Su interfaz la construye el sprint de frontend correspondiente, que puede ejecutarse en paralelo contra los contratos ya aprobados.

## Unidades de trabajo — Backend

| ID | Resultado observable | Deriva de | Depende de | Paralelizable con | Interfaz fijada | Criterio de cierre |
|---|---|---|---|---|---|---|
| UT-01 | Fundación compartida: columna generada `busqueda` en configuración `spanish`, índice GIN y el servicio de búsqueda del dominio | RF-007, RNF-001, MIG-005 | ninguna | ninguna | firma pública del servicio de búsqueda | migración aplica desde base limpia; con 30 000 documentos sembrados la consulta responde en menos de 2 segundos y `EXPLAIN ANALYZE` confirma uso del índice GIN, no recorrido secuencial |
| UT-02 | Búsqueda por contenido con paginación, filtros, alcance por rol y fragmento resaltado | RF-007, RNF-011, RNF-012 | UT-01 | UT-03, UT-04 | `GET /buscar` (contrato v1) | el término se pasa como parámetro enlazado y un término con sintaxis de inyección se trata como texto literal; el alcance por rol se aplica antes de calcular `total`; los documentos `ILEGIBLE` no aparecen; el resaltado se aplica **después** del escapado |
| UT-03 | Línea de tiempo del paciente con filtros de tipo y fecha | RF-008 | ninguna | UT-02, UT-04 | `GET /pacientes/{id}` (contrato v1) | orden `fecha_documento desc, id desc` verificado entre páginas; los documentos sin fecha van al final y quedan excluidos al filtrar por rango, salvo con `desde=sin-fecha`; los filtros se reflejan en la URL y sobreviven al refresco |
| UT-04 | Visor de documento y entrega controlada de la imagen original | RF-009 | ninguna | UT-02, UT-03 | `GET /documentos/{id}`, `GET /documentos/{id}/imagen` (contrato v1) | la imagen **nunca** se sirve desde una ruta pública estática: siempre pasa por la operación que evalúa permisos; el binario devuelto coincide en hash con el original; fuera de alcance devuelve `RECURSO_NO_ENCONTRADO`, no `NO_AUTORIZADO`; un documento `ILEGIBLE` explica que no tiene texto |
| UT-05 | Integración del sprint: rutas del dominio con sus parámetros de filtro y paginación | RF-007, RF-008, RF-009 | UT-02, UT-03, UT-04 | ninguna | rutas nombradas que F06 consume | los parámetros de filtro, orden y página se resuelven desde la URL, de modo que F06 pueda reflejarlos y conservarlos al navegar |
